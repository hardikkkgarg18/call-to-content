fastapi>=0.115.0
uvicorn[standard]>=0.30.6
openai>=1.30.0
pydantic>=2.10.0
python-dotenv>=1.0.1
httpx>=0.27.2

# Optional — only for the /enrich-profile endpoint (LinkedIn scraping).
# The core /analyze pipeline runs without it.
linkedin-api>=2.0.0
