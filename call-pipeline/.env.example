# Required — the core /analyze pipeline uses OpenAI.
OPENAI_API_KEY=your_openai_api_key_here

# Optional — only for the /enrich-profile endpoint (LinkedIn scraping).
# Leave blank to skip LinkedIn enrichment; /analyze still works.
LINKEDIN_EMAIL=
LINKEDIN_PASSWORD=
